# 运维手册（OPS）——数据安全与故障预案

> 本文档回答两个问题：云服务出问题时怎么办、平时需要多久检查一次。
> 结论先行：**日常几乎不需要维护**。应用已内置三层保护，你只需要记住「每周录周报时顺手看一眼」。

## 一、Supabase 故障预案

### 1. 应用已内置的保护机制（无需你操作）

| 场景 | 应用的自动行为 |
|------|----------------|
| 记录时恰好断网 | 数据先存本设备，标记「离线待同步」；网络恢复后**自动补传云端**，不丢数据 |
| Supabase 短暂无响应 | 页面顶部徽标变「连接失败」，可点「离线浏览」查看全部历史数据；恢复后自动同步 |
| 手机切后台 / 飞行模式回来 | 自动检测未同步改动并补传 |
| 删除操作在不同设备间冲突 | 墓碑机制保证已删除的记录不会在同步时「复活」 |
| 两台设备同时改同一条记录 | 按「最后修改时间」自动合并，本机最新编辑优先 |

### 2. 故障分级与应对

**级别 A：本地网络断开（最常见）**
- 现象：徽标显示「离线待同步」。
- 应对：什么都不用做。正常继续记录，联网后 1~2 秒内自动同步。
- 判断恢复：徽标变回「已同步 HH:MM」即成功。

**级别 B：Supabase 项目休眠（免费版特性）**
- 现象：超过 7 天没有任何读写，项目会自动休眠，首次访问报连接失败。
- 应对：刷新页面重试一次即可，首次唤醒约需 30~60 秒，之后恢复正常。
- 预防：只要你每周正常录周报，就永远不会触发休眠。

**级别 C：Supabase 平台故障（罕见）**
- 现象：所有设备都连接失败，且持续数小时。
- 应对：等待官方恢复即可（status.supabase.com 可查公告）。期间应用可离线浏览、离线记录，恢复后自动补传。你的数据不会因平台故障丢失。

**级别 D：极端情况——Supabase 项目被误删**
- 应对：重新创建 Supabase 项目 → SQL Editor 执行 `sql/setup.sql` → 把新项目的 URL 和 key 填入 `js/config.js` → 用最近一次「导出 JSON 备份」在网站「导入历史数据」中恢复。
- 因此：**定期导出 JSON 备份是唯一必须养成的习惯**（见上节）。

**级别 E：GitHub Pages 站点下线（仓库转私有）**
- 现象：线上网址 404，但已登录过的老设备仍可离线使用（v3.2.2 起 404 页面不会污染本地缓存）。
- 影响：Supabase 云端完全不受影响，数据零损失；但下线期间网站失去自然流量，需注意 Supabase 保活（见第 4 节）。
- 恢复：GitHub 仓库页 Settings → General → Danger Zone → Change visibility 改回 Public；若 Pages 被一并关闭，再到 Settings → Pages 重新选 main/root 开启，站点分钟级恢复，数据自动同步。
- 注意：API 方式切换需要有效 token，撤销 token 前先想清楚是否还需要远程下线/上线。

### 3. 备份策略（唯一需要你动手的事）

| 频率 | 动作 | 耗时 |
|------|------|------|
| 每季度一次（或每次批量录入大量数据后） | 网站内「导出 JSON 备份」，文件存到电脑 / 网盘 | 10 秒 |
| 平时 | 应用内已有 14 天未备份提醒（可关闭），按提醒操作即可 | 10 秒 |

数据流向、离线机制与恢复方法的完整说明见 [DATA.md](DATA.md)。

### 4. 长期不用时的保活方案

若计划连续 7 天以上不打开网站（如搁置、休假），Supabase 免费版会因无请求而休眠。收藏以下形式的链接，每 1~2 周点一次即可保活（只读拉取一次数据，不产生任何写入）：

```text
https://<你的项目域名>.supabase.co/rest/v1/rpc/sync_get?p_code=<你的访问口令>&apikey=<你的 anon key>
```

⚠️ 该链接内含访问口令，**切勿分享给他人**；保活链接的实例版本已交付项目所有者。也可以让 AI 助手代为保活（会话说「保活」即可）。

## 二、Supabase 后台巡检频率建议

| 检查项 | 建议频率 | 怎么看 |
|--------|----------|--------|
| 数据是否正常同步 | 每周录周报时顺手一次（推荐周日） | 不用登录 Supabase，看网站右上角徽标显示「已同步」即可 |
| Supabase 后台数据抽查 | 每月一次足够 | 登录 Supabase → Table Editor → `log_data` 表，看 `updated_at` 是否为最近日期、`payload` 里文章数是否符合预期 |
| 项目用量 / 休眠状态 | 每季度一次 | Supabase 控制台首页看 Project Usage；免费额度对单人使用完全够用 |
| JSON 备份导出 | 每季度一次 | 网站内「导出 JSON 备份」 |

**一句话原则**：网站徽标是「已同步」就不用管 Supabase；徽标异常超过 1 小时再登录 Supabase 后台排查。

## 三、常见问题速查

| 现象 | 原因 | 处理 |
|------|------|------|
| 打开网站显示 404 | 仓库被转为私有，或 Pages 被关闭 | 按级别 E 恢复公开并重开 Pages；老设备离线功能不受影响 |
| 换了手机登录，数据不全 | 旧设备还有未同步的改动 | 在旧设备联网打开网站，等徽标变「已同步」后新设备刷新 |
| 提示「口令已失效」 | 曾在后台改过口令 | 输入当前口令重新验证 |
| 图表不显示 | 浏览器禁用了 JS 或缓存异常 | 强制刷新一次（电脑 Ctrl+F5 / 手机清一次该站点缓存） |
| 添加到主屏幕后打不开 | PWA 缓存更新中 | 关掉重开一次，或等 1 分钟 |
