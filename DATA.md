# 数据存储与备份机制说明

> 本文档是项目四份文档（README / DEPLOY / OPS / DATA）之一，与「数据总览页 → 云端数据说明」弹窗内容同源。

> 本文档面向项目所有者，完整回答三个问题：**你记录的数据存在哪里？Supabase 出问题时数据去哪？如何备份与恢复？**
> 内容基于 `js/store.js` 的真实实现逻辑整理，非泛泛而谈。

---

## 〇、一图看懂数据流向

```
你在手机 / 电脑上记录数据（如今天发布的文章）
        │
        │ ① 立刻写入本机浏览器的 localStorage（glog_cache，秒级完成，断网也成功）
        ▼
   防抖约 2 秒，自动推送
        │
        │ ② 整包上传 Supabase 云端（口令校验通过后写库）
        ▼
Supabase 云数据库（主存储，跨设备共享、长期保存）
        │
        │ ③ 右上角徽标显示「已同步 HH:MM」= 全部完成
        ▼
定期 ④ 手动「导出 JSON 备份」→ 存电脑 / 网盘（人工兜底）
```

因此你的数据始终存在 **三个副本** 里：

| 副本 | 存放位置 | 谁在维护 | 核心作用 |
|------|----------|----------|----------|
| ① 本地缓存 | 每台设备浏览器的 localStorage（键名 `glog_cache`） | 应用自动读写，你无感 | 秒级保存、离线可用、断网暂存 |
| ② 云端主库 | 你自己的 Supabase 项目（`log_data` 表，单行整包 JSON） | 应用自动同步 | 跨设备共享、长期持久存储 |
| ③ 备份文件 | 你导出的 JSON 文件（电脑 / 网盘） | 你手动导出 | 终极兜底，可恢复到任何新环境 |

---

## 一、正常记录时，数据去了哪里

以「今天发布了一篇文章，我记录一下」为例，完整链路如下：

1. **录入瞬间**：你在手机或电脑上填写文章信息并保存，数据**第一时间写入当前这台设备浏览器的 localStorage**（键名 `glog_cache`，结构为 `{ payload, dirty, savedAt }`）。这一步是纯本地操作，即使此刻断网也会成功，所以不存在「点保存没反应数据就没了」的情况。
2. **自动上云**：约 2 秒防抖后，应用把**整包数据**（全部文章 + 全部周报 + 头像 + 墓碑记录 + 时间戳）通过 Supabase 的 `sync_save` 接口推送到云端。推送成功后，右上角徽标显示「已同步 HH:MM」——看到它就代表云端已经拿到最新数据。
3. **云端存什么**：Supabase 里只有两张表——`log_data`（你的全部日志数据，单行整包 JSON）和 `app_config`（口令的 bcrypt 哈希，明文永不落库）。两张表都启用了 RLS 行级安全并收回匿名直连权限，任何人拿着公开密钥也无法绕过口令直接读表。
4. **多设备关系**：手机和电脑各自维护一份独立的本地缓存，云端是唯一「事实标准」。任何设备登录后都会拉取云端全量数据对齐，所以两台设备看到的内容最终一致。

---

## 二、Supabase 异常或下线时，手动录入的数据存哪里

按故障严重程度分四个场景说明：

### 场景 1：你的网络断了 / 手机切后台（最常见）

数据照常**先写入本机 localStorage**，并标记为「离线待同步（dirty）」。应用会监听 `online`（网络恢复）和 `visibilitychange`（切回前台）事件，**检测到网络可用后自动补传云端**，全程不需要你做任何操作。判断是否恢复：右上角徽标从「离线待同步」变回「已同步 HH:MM」即成功。

### 场景 2：Supabase 项目休眠（免费版机制）

免费版规则：**项目连续 7 天没有任何请求会被自动暂停（休眠）**。休眠后：

- 数据**原样保留**在 Supabase 服务器上，不收费、不丢失；
- 首次访问会连接失败，刷新页面重试即可，首次唤醒约需 30~60 秒，之后恢复正常；
- 只要你每周正常录周报，就永远不会触发休眠；长期搁置的预防方案见第五节「保活」。

### 场景 3：Supabase 平台故障（罕见）

所有设备都连接失败且持续数小时。此时应用可**离线浏览全部历史数据、离线继续记录**——因为每台登录过的设备 localStorage 里都有一份**最近一次同步的完整副本**。平台恢复后自动合并补传，数据不丢。可到 status.supabase.com 查看官方公告。

**新设备 / 清过缓存的浏览器也不怕**（v3.10+）：云端连不上且本地无缓存时，网页会自动加载随站点内置的最新 GitHub 备份（`backups/latest-cloud.json`），右上角显示蓝色「备份模式」徽标，数据照常完整展示；云端恢复后自动回到实时同步。

### 场景 4：极端情况——Supabase 项目被误删

云端没了，但你有三道防线：

1. **每台老设备的 localStorage 仍存有完整数据**（最近一次同步的副本）；
2. **你的 JSON 备份文件**；
3. **随站点内置的最新 GitHub 备份**（`backups/latest-cloud.json`，v3.10+ 任何新设备打开网页即自动展示，无需任何操作）。

恢复路径：重新创建 Supabase 项目 → SQL Editor 执行 `sql/setup.sql` → 把新项目的 URL 和 key 填入 `js/config.js` → 用备份文件在网站「导入历史数据」恢复（或让任一老设备联网打开网站，自动把整包数据上传到新库）。

### ⚠️ 必须知道的 localStorage 边界

- localStorage 属于「**这台设备的这个浏览器**」：换浏览器、开隐私/无痕模式、清除浏览器数据、手机恢复出厂，本地缓存就没了；
- 所以 localStorage 是**缓存，不是备份**——但这没有关系：只要数据已经同步上云（徽标显示「已同步」），清掉本地缓存毫无损失，重新输入口令即可从云端拉回全量；
- 真正有丢失风险的窗口只有一种：**刚记完数据还没来得及同步（徽标未显示已同步）＋ 又恰好清了本地缓存**。养成「记录完瞄一眼徽标」的习惯即可完全规避；
- 终极保险永远是 JSON 备份文件。

---

## 三、多设备数据为什么不会互相覆盖

- **时间戳合并**：每条记录都带毫秒级时间戳 `_ts`，多端合并时同一条记录取最新版本（本机刚编辑过的优先）；
- **墓碑机制**：删除操作会写入 tombstones（保留最近 300 条），保证已删除的记录不会在另一台设备同步时「复活」；
- **整包推送 + 防抖**：避免频繁请求，也保证云端任意时刻都是完整一致的全量数据。

---

## 四、备份体系与恢复方法

| 项目 | 说明 |
|------|------|
| 导出入口 | 数据总览页 → 数据管理卡 →「导出 JSON 备份（本机快照）」或「导出云端全量数据」（文件名自动含日期） |
| 两种备份文件 | ① 本机快照：导出当前设备内存中的数据（`_meta.version 3.0`）；② 云端全量（v3.9+）：点击后直连 Supabase 拉取权威整包（`_meta.source: "cloud"`），与本机缓存状态无关，云端此刻有多少就导多少；两种文件均可用「导入历史数据」合并恢复 |
| 建议频率 | 每季度一次；批量录入大量数据后加做一次；应用内置 14 天未备份自动提醒 |
| 存放位置 | 电脑本地 + 网盘各一份（双介质，防止单点丢失） |
| 恢复方式 ① | 网站「导入历史数据」选择备份文件（内置字段级清洗，防恶意数据注入） |
| 恢复方式 ② | 技术流可用脚本直接调用 RPC 接口上传整包（适合批量恢复） |

---

## 五、Supabase 官网需要你做的事（巡检清单）

| 频率 | 动作 | 说明 |
|------|------|------|
| 平时 | **什么都不用做** | 徽标显示「已同步」即一切正常 |
| 每 1~2 周 | 保活一次 | 长期不打开网站时，点一次下面的保活链接即可（正常使用网站本身就是保活） |
| 每月 | 后台抽查一次 | 登录 Supabase → Table Editor → `log_data` 表，看 `updated_at` 是否为最近日期 |
| 每季度 | 导出云端全量备份 + 看一次用量 | 控制台首页 Project Usage，免费额度对单人使用完全够用 |
| 仅异常时 | Restore 唤醒 / 改口令 | 休眠时在 Supabase 面板点 Restore（1~2 分钟复活）；改口令重跑 `setup.sql` 第 ② 段 |

**保活收藏夹链接**（只读拉取一次数据以保持项目活跃；⚠️ 链接内含访问口令，切勿转发给他人）：

```
https://<你的项目域名>.supabase.co/rest/v1/rpc/sync_get?p_code=<你的访问口令>&apikey=<你的 anon key>
```

---

## 六、常见问题

**Q1：换新手机 / 新电脑，数据怎么过来？**
新设备浏览器打开网站 → 输入访问口令 → 自动拉取云端全量数据，与老设备完全一致。

**Q2：清了浏览器缓存 / 换了个浏览器，数据会丢吗？**
只丢本地缓存。云端数据不受影响，重新输入口令即全量恢复。

**Q3：隐私模式 / 无痕窗口里记录的数据呢？**
无痕窗口关闭即全部丢失（本地存储受限且大概率未及同步）。重要记录请勿在无痕模式下操作。

**Q4：两台设备同时记录会冲突吗？**
不会。按时间戳自动合并，删除有墓碑保护，无需人工干预。

**Q5：需要定期登录 Supabase 官网吗？**
不需要。只有三种情况才用得到官网：休眠恢复（点 Restore）、改口令（重跑 SQL）、查看用量。

**Q6：数据量大了手机会卡吗？**
不会。整包数据为 KB 级（当前 87 篇文章 + 29 周报约 32KB），免费版 500MB 容量按当前节奏可用十年以上。
